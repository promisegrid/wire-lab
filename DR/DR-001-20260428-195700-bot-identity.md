# DR-001 - Perplexity Computer bot identity

DR-ID: DR-001-20260428-195700
Date: 2026-04-28 19:57:00
Asked by: stevegt+ppx@t7a.org (stevegt-via-perplexity)
State: decided
Question: What email identity and branch prefix should the Perplexity Computer bot use when committing to this repo?
Why this blocks progress: AGENTS.md requires every DR/DI Author and Asked by field to use `email (FirstName)` format. Without a settled bot identity, the bot cannot author any DR or DI record in compliance with the protocol; therefore no work can begin.
Affects: bot's `.git/config` in any clone; all branch names the bot creates; all `Author` and `Asked by` fields the bot writes; commit author metadata; future cross-references in code comments (`Source: DI-NNN-…`).
Unblocks: protocols/wire-lab.d/TODO/TODO-20260429-030146-perplexity-computer-onboarding.md (subtask 001.1) and every subsequent bot-authored task.
Waiting on: DI-001-20260428-195700

## Candidate alternatives considered

- (a) `ppx@t7a.org (Perplexity)` → derived `<user>` = `ppx`. Cleanest match to the mob-consensus convention but requires a separate mailbox.
- (b) `stevegt+ppx@t7a.org (stevegt-via-perplexity)` → derived `<user>` = `stevegt+ppx`, branch prefix overridden to `ppx/`. Selected.
- (c) `perplexity@t7a.org (Perplexity)` → derived `<user>` = `perplexity`. More descriptive but longer prefix, and conflates the bot's vendor with its operator.

## Decision

Use `stevegt+ppx@t7a.org (stevegt-via-perplexity)` for the bot's email and identity label. Branch prefix is `ppx/{twig}` — this overrides the mob-consensus default of `stevegt+ppx/{twig}` because Steve specifically asked for the short `ppx/` prefix.

## Linked DI

DI-001-20260428-195700 (in `protocols/wire-lab.d/TODO/TODO-20260429-030146-perplexity-computer-onboarding.md`)

## Related commits

To be appended on merge of `ppx/dr-001-bootstrap`.

## Last updated

2026-04-28 19:57:00
