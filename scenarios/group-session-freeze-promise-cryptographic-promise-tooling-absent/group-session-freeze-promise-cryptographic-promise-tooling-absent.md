# Cryptographic promise tooling absent

## Scenario ID

group-session-freeze-promise-cryptographic-promise-tooling-absent

## Source / Provenance

- Source type: mined simulation row
- Source path: `simulations/SIM-bohof-group-session-freeze-promise/SCENARIOS.md`
- Source simulation: `SIM-bohof-group-session-freeze-promise/`
- Source row/title: Cryptographic promise tooling absent
- Source DI / TODO / TE: `DI-faros`; `DI-vabor`; `DI-botup`; `DI-nanih`

## Purpose

Transform the source row into reusable root comparison pressure so multiple
simulations can be evaluated against the same question rather than only inside
`SIM-bohof-group-session-freeze-promise/`.

## Setup

The desired signed-promise tooling is not yet available.

## Stimulus

Run the candidate simulation against this source test: Whether a temporary DI/DR-backed promise is acceptable or freeze must wait.

## Expected Pressure

The project needs a fallback policy for early specimens.
