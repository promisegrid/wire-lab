package compute

// Kind names the compute resource app role used by the POC9 promise-token demo.
// Intent: Treat computation access and returned results as promises whose
// keep/break history can affect local trust. Source: DI-tugih
const Kind = "compute"
