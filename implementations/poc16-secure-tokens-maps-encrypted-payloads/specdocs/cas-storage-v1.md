# cas_storage_v1

Agents use `cas_storage_v1` to promise content-addressed storage, retrieval,
replication, retention, and local garbage-collection behavior. Payloads carry
content CIDs, exact-byte expectations, reciprocal economics, and optional
capability tokens; storage trust remains a local judgment by each agent.
