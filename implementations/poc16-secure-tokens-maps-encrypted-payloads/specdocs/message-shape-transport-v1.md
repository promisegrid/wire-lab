# message_shape_transport_v1

Specimen pCID for `grid([42(pCID), payload])`. It models a transport- or
session-authenticated profile where the pCID defines how the payload is
interpreted and no universal envelope proof slot is present.
