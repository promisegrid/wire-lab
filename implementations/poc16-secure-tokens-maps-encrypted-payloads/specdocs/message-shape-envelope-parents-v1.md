# message_shape_envelope_parents_v1

Specimen pCID for `grid([42(pCID), parents, payload, proof])`. Parent links are
wire-visible envelope slots and the pCID defines how they participate in the
signable view.
