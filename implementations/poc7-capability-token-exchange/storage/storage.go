package storage

// Kind names the storage resource app role used by the POC7 promise-token demo.
// Intent: Treat storage access as a promise issued by the storage-controlling
// agent, not as a global authorization bit enforced by the kernel. Source: DI-tugih
const Kind = "storage"
