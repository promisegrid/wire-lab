package data

// Kind names the data resource app role used by the POC7 promise-token demo.
// Intent: Keep "data" as an app-level resource promise rather than a kernel
// gatekeeping category, so Alice can locally choose when to send bytes based on
// Bob's promise history. Source: DI-tugih; DI-tanat
const Kind = "data"
