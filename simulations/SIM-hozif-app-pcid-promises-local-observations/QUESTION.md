# Question

Can PromiseGrid replace RPC-like app/kernel registration with one stable pCID-selected local promise protocol whose payload kinds let an app promise a local kernel which pCID-selected messages it will receive or handle, let the kernel promise best-effort delivery and local evidence behavior, and let each agent record kept, refused, broken, or timed-out outcomes only as its own local observation?
