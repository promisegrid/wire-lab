# SIM-suzuf-gordian-universal-envelope-negative-control: Gordian universal-envelope negative control

This simulation is a standalone negative-control specimen. It asks what happens
if Gordian-style subject/assertion/proof structure becomes the universal outer
envelope rather than a pCID-owned payload family. Source: `DI-nohir`.

The local draft spec is
`protocols/grid-envelope.d/specs/grid-envelope-draft.md`.

Primary comparison targets: `SIM-fitin` and the minimal-grid family.
