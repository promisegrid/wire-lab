# Question

Is a UCAN-like "signed body plus sibling proof material" payload contract a
better PromiseGrid shape than either universal outer proof slots or an
atproto-like reserved proof slot with an explicit signable view? Source:
`DI-nohir`.
