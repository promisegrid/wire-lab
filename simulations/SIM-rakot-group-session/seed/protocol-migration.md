# Group-session lineage migration

The `group-session.d` lineage tree moved from
`SIM-piloh-turns-149-208-recovery/protocols/group-session.d/` into
`SIM-rakot-group-session/protocols/group-session.d/` during `rusis.5`.

This local note preserves the `group-session` lineage intent previously carried
in the mixed umbrella records: the old `protocol-set.md` treated
`group-session` as one independent specimen inside the umbrella sim, and the
old `seed/protocol-tree-migrations.md` recorded its migration provenance as a
lineage-specific row rather than as rooted harness state. That provenance now
lives with the `group-session` lineage itself. Source: `DI-firiv`, `DI-rugig`.
