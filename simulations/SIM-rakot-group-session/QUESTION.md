# Question

Which `group-session` design choices remain worth carrying forward when that
lineage is compared against other candidate PromiseGrid lineages instead of
being treated as the default answer? Source: `DI-limom`, `DI-rugig`.
