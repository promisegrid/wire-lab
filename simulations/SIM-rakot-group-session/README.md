# SIM-rakot: Group session

This simulation carries the `group-session` lineage as an independent candidate
for message-framing, membership, and related transport decisions. It exists so
those design choices can evolve on their own merits rather than as implicit
defaults for the rest of wire-lab. Source: `DI-limom`, `DI-rugig`.
