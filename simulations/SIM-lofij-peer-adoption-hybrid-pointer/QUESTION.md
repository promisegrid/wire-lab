# Question

Can a compact adoption promise plus optional richer pointer give PromiseGrid
peers enough adoption evidence without requiring every use case to carry the
same metadata weight?

Source: `DI-fibuv`.
