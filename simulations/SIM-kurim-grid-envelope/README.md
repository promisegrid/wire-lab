# SIM-kurim: Grid envelope

This simulation reserves an independent lineage for the
`grid([pcid, payload])` working hypothesis. It keeps that envelope idea visible
as a live comparative candidate without treating it as the settled PromiseGrid
wire format. Source: `DI-limom`, `DI-rugig`.
