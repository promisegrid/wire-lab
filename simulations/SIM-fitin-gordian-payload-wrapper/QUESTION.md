# Question

Can Gordian-style subject/assertion/proof structure improve PromiseGrid
auditability when it is confined to a pCID-owned payload/wrapper family, or
does it still add more wrapper complexity than the minimal grid needs? Source:
`DI-nohir`.
