# Question

Should PromiseGrid prefer a small fixed grid-envelope header with a pCID-defined
variable body, so later peers retain generic audit and promise interpretation
without freezing every inner body shape?

Source: `DI-tavaz`.
