# Question

Should PromiseGrid prefer a Ceramic-like proof wrapper selected by the payload
pCID, where the wrapper signs linked content rather than carrying a proof in the
same payload bytes, when the goal is to keep the universal outer grid tiny and
avoid self-referential signing rules? Source: `DI-nohir`.
