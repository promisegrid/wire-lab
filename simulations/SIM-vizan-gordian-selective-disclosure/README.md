# SIM-vizan-gordian-selective-disclosure: Gordian selective-disclosure probe

This simulation is a standalone grid-envelope specimen. It keeps the universal
outer grid minimal, but adds a Gordian-style payload/wrapper family that is
optimized for selective disclosure and reveal-pressure rather than only basic
subject/assertion/proof structure. Source: `DI-nohir`.

The local draft spec is
`protocols/grid-envelope.d/specs/grid-envelope-draft.md`.

Primary comparison targets: `SIM-fitin` and `SIM-suzuf`.
