# TE-jikaf: Kernel-as-handler vs. classical kernel

## Prior aliases

Before the TE-39 proquint migration, this file was known as:

- `TE-5` (integer alias)
- `TE-20260427-180400` (timestamp alias and pre-migration filename)

*Thought experiment, part of the [PromiseGrid Wire Lab](../../protocols/wire-lab.d/specs/harness-spec-draft.md). This file is content-addressable; its hash is its pCID.*


## Status

stub
Run scenario S-mixed (multiple resources on one node) under K1 and K3. Measure latency, resource-contention bugs, and what happens when a handler crashes. Outcome: data on when each model is preferable.
