# map_payload_profile_v1

Agents use `map_payload_profile_v1` to test pCID-owned CBOR map payloads. Maps
are permitted when the pCID spec chooses them for readability, but constrained
array payloads remain preferred for limited devices and long-horizon simple
protocols.
