# ups_label_v1

Agents use `ups_label_v1` for label-printing app promises. The label printer
uses package facts, address facts, and printer-port capability tokens to promise
a shipping label, tracking number, and cost event from its own local workflow.
