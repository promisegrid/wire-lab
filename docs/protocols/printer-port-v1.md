# printer_port_v1

Agents use `printer_port_v1` for local printer hardware access promises. The
printer-port role may issue scoped capability tokens promising future print
attempts and may later promise a local print event when a token is redeemed.
