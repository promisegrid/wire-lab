# TODO-rohub: Dogfood message tool name and collaborator permission

## Status

Open. This TODO owns turn-178 meta-questions that should not remain only in
closed replay notes. It is a harness / dogfood coordination owner, not a
PromiseGrid protocol-design question home. Source: `DI-lusum`.

## Scope

- Decide whether `pgmsg` is the right dogfood message-tool name, or choose a
  different name before implementation or user-facing docs rely on it.
- Preserve the collaborator permission constraint: do not hardcode a named
  collaborator or permanent handle in committed docs, examples, tools, or
  specimens until explicit permission and a chosen handle exist.
- Do not reopen meta-questions that already have owners: reading promisebase
  before adopting it is routed to TODO-kituj / `DR-tumus` and later promisebase
  review; TE-sihih one-big-versus-split scope is resolved by TODO-vunub's scope
  contraction and successor-owner split.

## Subtasks

- [ ] rohub.1 Decide whether `pgmsg` remains the dogfood message-tool name or
  whether another name should be used before implementation or public docs.
- [ ] rohub.2 Confirm collaborator permission and chosen handle before any
  committed artifact names the collaborator; until then use
  `[redacted-collaborator]` or generic collaborator labels only.
- [ ] rohub.3 When dogfood messaging implementation work opens, move or
  duplicate the `rohub.1` and `rohub.2` constraints into that implementation
  TODO, then close this routing TODO.

## Routed Elsewhere

- Turn-178 "connect GitHub / read promisebase before drafting" is closed for
  replay purposes by later promisebase review and routed design work in
  TODO-kituj / `DR-tumus`.
- Turn-178 "TE-sihih as one big TE or split" is closed for replay purposes by
  TODO-vunub's smaller TE-sihih landing and the later split into successor TODOs
  and simulations.

## Question Log

- 2026-05-17: Turn 178's `pgmsg` tool-name question and collaborator
  permission constraint are routed here so `UT-178.k` does not close solely as a
  tactical-deferral lesson. Source: `DI-lusum`.
- 2026-05-17: Turn 184 and turn 186 show the same collaborator-permission issue
  as a propagation hazard: a protected collaborator name can leak when an open
  question is written, persisted, or re-quoted. This TODO owns that constraint
  until explicit permission and a chosen handle exist. Source: `DI-nulak`.

## Decision Intent Log

The creation and routing decision for this TODO is recorded as `DI-lusum` in
`TODO-juhub-turns-149-208-chronological-rewalk.md`. Future locked decisions
local to dogfood tool naming or collaborator permission should be appended here.
