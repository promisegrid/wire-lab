# SIM-piloh migration archive

This subtree will receive dissolved umbrella-simulation provenance from
`SIM-piloh-turns-149-208-recovery/` as later `rusis.*` passes split the mixed
recovery sim into independent lineages. It is historical migration evidence,
not a continuing simulation root. Source: `DI-limom`, `DI-rugig`.
