# SIM-piloh migration archive

This subtree now holds dissolved umbrella-simulation provenance from
`SIM-piloh-turns-149-208-recovery/` after the `rusis.*` split passes separated
the mixed recovery sim into independent lineages. It is historical migration
evidence, not a continuing simulation root. Source: `DI-limom`, `DI-rugig`,
`DI-turud`, `DI-jugub`.
