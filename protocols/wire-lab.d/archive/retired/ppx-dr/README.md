# Retired `ppx-dr` archive

This subtree will receive the retired `ppx-dr` lineage history as later
`rusis.*` passes remove `ppx-dr` from the active simulation set. It preserves
proposal and provenance evidence without keeping `ppx-dr` as an active
candidate lineage. Source: `DI-limom`, `DI-rugig`.
